on:
  push:
    branches:
      - main
name: Build Angular
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: use Node version 18
        uses: actions/setup-node@v3
        with:
          node-version: 18.x

      - name: npm install and npm run build
        working-directory: './'
        run: |
          npm install --legacy-peer-deps
          npm run build
          echo ${{ secrets.AZURE_BLOB_STORAGE_CONNECTSTRING }}

      - uses: LanceMcCarthy/Action-AzureBlobUpload@v2
        name: Uploading to Azure storage...
        with:
          source_folder: ./dist
          container_name: '$web'
          connection_string: DefaultEndpointsProtocol=https;AccountName=647project1;AccountKey=XCgViVuV7F/roza6HNXi3KnvSGtvomuABMNGvbyMi1Q3VMVTQHFYcKpB2THAXA79H6CxzqA4cZhX+AStzAv34A==;EndpointSuffix=core.windows.net
          clean_destination_folder: 'true'
          delete_if_exists: 'true'